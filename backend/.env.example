# Backend environment variables - copy to .env and fill values
# Do NOT commit your real keys into source control.

# Supabase
SUPABASE_URL=
SUPABASE_KEY=

# Frontend URL allowed for CORS
FRONTEND_URL=http://localhost:3000

# Generative AI (Gemini) key - set this to your API key on the server
# IMPORTANT: Do NOT commit this key. Use your deployment's secret management.
GEMINI_API_KEY=

# Optional: override the generative API endpoint if needed
# Example for Google Generative API (may require different payload format):
GENERATIVE_API_URL=https://generativeai.googleapis.com/v1beta2/models/text-bison-001:generateText
PORT=3001
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key
JWT_SECRET=your_jwt_secret_key_change_this_in_production
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_email_password
FRONTEND_URL=http://localhost:3000
