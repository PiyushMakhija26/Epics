# Backend environment variables - copy to .env and fill values
# Do NOT commit your real keys into source control.

PORT=3001
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/civicserve

# JWT (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email (SMTP) - leave empty to use Ethereal test account
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000
